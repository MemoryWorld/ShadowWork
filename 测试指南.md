# ShadowWork 本地测试指南

## 🎯 测试目标

本测试将验证 ShadowWork 的所有核心功能是否正常工作。

---

## ⚠️ 开始前的准备

### 1. 清理旧的 node_modules（如果有问题）

在项目目录下，以**管理员身份**打开 PowerShell 或 CMD，运行：

```powershell
# PowerShell
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item package-lock.json -ErrorAction SilentlyContinue

# 或者在 CMD
rmdir /s /q node_modules
del package-lock.json
```

### 2. 安装依赖

```bash
npm install
```

**预计时间**: 2-3 分钟  
**预期输出**: 看到 "added XXX packages" 表示成功

如果遇到权限问题，请：
1. 以管理员身份运行终端
2. 或者关闭所有占用文件的程序（如 VS Code）

---

## 📝 测试步骤

### 测试 1: 启动开发服务器 ✅

**命令**:
```bash
npm run dev
```

**预期结果**:
```
- Local:        http://localhost:3000
- Ready in XXXms
```

**验证点**:
- [ ] 看到 "Ready" 消息
- [ ] 没有编译错误
- [ ] 端口 3000 被占用

**如果失败**:
- 检查端口 3000 是否被占用: `netstat -ano | findstr :3000`
- 查看错误信息并修复

---

### 测试 2: 访问着陆页 🏠

**操作**:
1. 打开浏览器（Chrome 或 Edge）
2. 访问 `http://localhost:3000`

**预期结果**:
- [ ] 页面加载成功（无白屏）
- [ ] 看到 "ShadowWork" 标题
- [ ] 看到渐变背景
- [ ] 看到 "Zero-Resume, Proof-of-Work" 大标题
- [ ] 有一个蓝色的 "Start Challenge" 按钮

**测试什么**:
✅ Next.js 服务器正常运行  
✅ 页面路由正常  
✅ Tailwind CSS 样式加载  
✅ React 组件渲染  

**截图位置**: 可以保存为 `test-1-landing-page.png`

---

### 测试 3: 检查 COOP/COEP 头部 🔐

**操作**:
1. 在着陆页按 `F12` 打开开发者工具
2. 点击 "Network" 标签
3. 刷新页面 (`F5`)
4. 点击第一个请求（localhost）
5. 查看 "Headers" → "Response Headers"

**预期结果**:
```
cross-origin-embedder-policy: require-corp
cross-origin-opener-policy: same-origin
```

**测试什么**:
✅ Middleware 正常工作  
✅ 全局头部注入成功  
✅ WebContainer 所需的 SharedArrayBuffer 支持已启用  

**这是最关键的测试！** 如果这两个头部不存在，WebContainer 将无法启动。

---

### 测试 4: 进入挑战模式 🎮

**操作**:
1. 点击 "Start Challenge" 按钮

**预期结果**:
- [ ] URL 变为 `/challenge?mock=true`
- [ ] 看到 "Loading challenge..." 提示（1秒）
- [ ] 看到 "Booting WebContainer..." 提示（3-5秒）
- [ ] 最终显示编辑器界面

**测试什么**:
✅ 路由跳转正常  
✅ Mock 模式 API 正常  
✅ Task 数据加载成功  

**如果卡在 "Loading challenge..."**:
- 打开控制台查看错误
- 检查 `/api/generate-task?mock=true` 是否返回数据

---

### 测试 5: WebContainer 启动 🚀

**操作**:
等待 WebContainer 启动（3-5秒）

**预期结果**:
- [ ] "Booting WebContainer..." 消失
- [ ] 显示三栏布局：
  - 左侧: 文件列表
  - 中间: 代码编辑器
  - 右侧: 输出面板
- [ ] 看到文件列表中的文件（如 `server.js`）

**控制台检查**:
打开浏览器控制台 (`F12` → Console)，应该看到：
```
[WebContainer] Booting...
[WebContainer] Boot successful
[Workspace] Loading task files...
[Workspace] Installing dependencies...
[Workspace] Task loaded successfully
```

**测试什么**:
✅ WebContainer API 初始化成功  
✅ SharedArrayBuffer 可用（依赖测试 3）  
✅ 虚拟文件系统挂载  
✅ npm install 在浏览器中执行  

**这是核心功能！** 如果失败：
- 确认测试 3 的头部存在
- 确认使用 Chrome/Edge 浏览器（Firefox 不支持）
- 查看控制台的详细错误

---

### 测试 6: rrweb 录制功能 🎥

**操作**:
WebContainer 启动后，观察顶部状态栏

**预期结果**:
- [ ] 看到红色闪烁的圆点
- [ ] 看到文字 "Recording (XXX events)"
- [ ] 事件数量随着操作增加

**操作验证**:
1. 在编辑器中输入一些字符
2. 移动鼠标
3. 滚动页面
4. 观察事件数量增加

**控制台检查**:
```
[useRecorder] Starting recording with optimization
```

**测试什么**:
✅ rrweb 库加载成功  
✅ 录制自动启动  
✅ 采样配置生效（鼠标移动不会导致事件暴增）  
✅ 事件计数器正常  

---

### 测试 7: Monaco 编辑器 ✏️

**操作**:
1. 点击左侧文件列表中的 `server.js`
2. 在编辑器中修改代码（如添加注释）

**预期结果**:
- [ ] 文件内容显示在中间编辑器
- [ ] 可以正常输入文字
- [ ] 有语法高亮（JavaScript）
- [ ] 有代码自动补全提示

**测试什么**:
✅ Monaco Editor 加载成功  
✅ 文件切换功能正常  
✅ 编辑器绑定到虚拟文件系统  
✅ 语法高亮正常  

---

### 测试 8: 代码执行 ▶️

**操作**:
1. 点击顶部的 "Run" 按钮
2. 观察右侧输出面板

**预期结果**:
- [ ] 右侧面板显示 "Running..."
- [ ] 看到代码执行输出
- [ ] 如果有错误，显示错误信息

**测试什么**:
✅ WebContainer spawn 命令正常  
✅ Node.js 代码可以在浏览器中执行  
✅ 输出流捕获正常  

**注意**: Mock 任务可能会失败（因为缺少 Redis），这是正常的！重要的是看到尝试运行。

---

### 测试 9: 测试运行 🧪

**操作**:
1. 点击顶部的 "Test" 按钮
2. 观察输出

**预期结果**:
- [ ] 右侧面板显示测试运行状态
- [ ] 显示测试结果

**测试什么**:
✅ npm test 命令执行  
✅ 测试框架集成  

---

### 测试 10: 提交流程 📤

**操作**:
1. 点击顶部的 "Submit" 按钮
2. 等待处理

**预期结果**:
- [ ] 看到提示 "Challenge submitted successfully!"
- [ ] 自动跳转到 `/success` 页面
- [ ] 成功页面显示绿色对勾
- [ ] 显示 "Challenge Submitted!" 标题

**控制台检查**:
```
[Workspace] Submitting solution...
[submit] Using Mock Mode (Supabase not configured)
[Workspace] Submission successful
```

**测试什么**:
✅ rrweb stopAndGetEvents 正常  
✅ API 提交请求成功  
✅ 路由跳转正常  

**注意**: 
- 如果未配置 Supabase，会模拟提交（这是正常的）
- 如果配置了 Supabase，会实际上传录像文件

---

### 测试 11: Slack 通知（可选）⚠️

**前提**: 需要配置 `SLACK_WEBHOOK_URL`

如果已配置，提交后：
- [ ] 检查 Slack 频道
- [ ] 看到新消息通知
- [ ] 验证消息内容：
  - 候选人 Hash（匿名）
  - 难度级别
  - 技术栈
  - **无** PR ID、Repo 名称

**测试什么**:
✅ Slack Webhook 集成  
✅ 隐私保护机制  
✅ 通知格式正确  

---

## 📊 测试结果汇总

完成上述测试后，填写下表：

| 测试项 | 状态 | 说明 |
|--------|------|------|
| 1. 开发服务器启动 | ⬜ | |
| 2. 着陆页加载 | ⬜ | |
| 3. COOP/COEP 头部 | ⬜ | **关键** |
| 4. 挑战模式跳转 | ⬜ | |
| 5. WebContainer 启动 | ⬜ | **核心** |
| 6. rrweb 录制 | ⬜ | |
| 7. Monaco 编辑器 | ⬜ | |
| 8. 代码执行 | ⬜ | |
| 9. 测试运行 | ⬜ | |
| 10. 提交流程 | ⬜ | |
| 11. Slack 通知 | ⬜ | 可选 |

---

## 🎯 测试覆盖范围

### 已测试的功能模块

✅ **Module A: 基础设施** (测试 3)
- Middleware 全局头部注入
- SharedArrayBuffer 支持

✅ **Module B: 数据管道** (测试 4)
- Mock 模式 API
- Task 数据加载

✅ **Module C: 沙箱** (测试 5-9)
- WebContainer 启动
- rrweb 录制优化
- Monaco 编辑器
- 代码执行

✅ **Module D: 用户流程** (测试 2, 4, 10)
- 着陆页
- 挑战页
- 提交流程
- 成功页

✅ **隐私保护** (测试 11)
- Slack 匿名通知

---

## 🐛 常见问题排查

### Q1: WebContainer 无法启动

**症状**: 一直显示 "Booting WebContainer..."

**排查步骤**:
1. 检查测试 3 - COOP/COEP 头部是否存在
2. 确认浏览器是 Chrome 或 Edge（最新版）
3. 打开控制台查看错误信息
4. 验证 `middleware.ts` 文件存在

### Q2: 页面白屏

**症状**: 访问 localhost:3000 显示空白

**排查步骤**:
1. 检查控制台是否有 JavaScript 错误
2. 确认 `npm run dev` 显示 "Ready"
3. 尝试刷新页面 (Ctrl+F5)
4. 检查浏览器兼容性

### Q3: Mock 数据加载失败

**症状**: 显示 "Failed to load task"

**排查步骤**:
1. 确认 `src/data/mock-task.json` 文件存在
2. 检查控制台 Network 标签，查看 `/api/generate-task` 请求
3. 查看服务器终端是否有错误

### Q4: rrweb 不录制

**症状**: 顶部没有显示 "Recording"

**排查步骤**:
1. 检查控制台是否有 rrweb 相关错误
2. 确认 `node_modules` 中有 `rrweb` 包
3. 重新安装: `npm install`

---

## 📸 建议截图

为了记录测试结果，建议保存以下截图：

1. **test-1-landing.png** - 着陆页
2. **test-2-headers.png** - Response Headers (COOP/COEP)
3. **test-3-editor.png** - 编辑器界面
4. **test-4-recording.png** - 录制状态
5. **test-5-console.png** - 控制台日志
6. **test-6-success.png** - 成功页面

---

## ✅ 测试完成确认

当所有测试通过后，你已经验证了：

✅ Next.js 14 App Router 正常工作  
✅ Middleware 全局头部注入成功  
✅ WebContainer 可以在浏览器中运行 Node.js  
✅ rrweb 录制功能正常且优化生效  
✅ Monaco 编辑器集成完美  
✅ API 路由响应正确  
✅ 用户流程完整流畅  
✅ 隐私保护机制到位  

---

## 🎉 恭喜！

如果所有测试都通过，说明 ShadowWork 项目已经完全可用！

你可以：
1. 继续开发新功能
2. 部署到 Vercel
3. 配置完整的 Supabase 和 OpenAI
4. 邀请其他人测试

---

## 📞 需要帮助？

如果遇到问题，请：
1. 查看控制台错误信息
2. 参考 `README.md` 的故障排除部分
3. 检查 `ARCHITECTURE.md` 了解系统架构

